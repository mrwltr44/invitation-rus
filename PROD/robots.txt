# Страницу из поиска убирает мета-тег noindex в index.html.
# Обход здесь намеренно НЕ запрещён: под запретом краулер не смог бы прочитать
# сам noindex, а ссылки на страницу всё равно попали бы в выдачу — без описания.
# Заодно остаются рабочими превью в мессенджерах: они тоже ходят краулером.

User-agent: *
Allow: /
