# Гости званные, да желанные!

Веб-приглашение на юбилей в русском стиле. Одна страница, вертикальная прокрутка, мобильный формат.

**Когда:** 22 августа, 15:00
**Где:** палаты купеческие «Ваша Гавань», Речной проезд, 19, Жуковский

## Что внутри

```
index.html            вся разметка
assets/css/style.css  вёрстка по координатам макета
assets/js/main.js     появление блоков при прокрутке
assets/img/           иллюстрации из Figma, сжатые в WebP
assets/fonts/         сюда лягут лицензионные шрифты, когда придут
_docs/design-map.md   соответствие блоков вёрстки узлам Figma
```

## Как устроена вёрстка

Макет мобильный, 402×3134. Все размеры в CSS заданы **в единицах макета** и пересчитываются одной переменной:

```css
--w: min(100vw, 430px);   /* ширина сцены */
--u: calc(var(--w) / 402); /* цена одной единицы макета */
```

Дальше любой размер пишется как `calc(31 * var(--u))` — это ровно 31 пиксель исходного макета. Страница масштабируется целиком, пропорции не плывут ни на узком экране, ни на широком.

Блоки позиционированы абсолютно, координаты взяты из Figma один в один. У каждого элемента в разметке есть `data-node` с идентификатором узла — по нему всегда можно вернуться к исходнику.

## Шрифты

В макете использованы **Yauza TYGRA** (заголовки, дата, время) и **Radiant** (основной текст). Оба лицензионные, в свободном доступе их нет.

Сейчас подключены открытые аналоги из Google Fonts: `Cormorant Unicase` и `PT Serif`. Начертание близкое по духу, но не совпадает буква в букву.

Когда придут файлы от дизайнера:

1. Положить `.woff2` в `assets/fonts/`.
2. Добавить в начало `style.css` блоки `@font-face` для обоих семейств.
3. Убрать `<link>` на Google Fonts из `index.html`.

Стек в CSS уже написан так, что оригиналы встанут первыми и подхватятся сами:

```css
--font-display: "Yauza TYGRA", "Cormorant Unicase", ...;
--font-body: "Radiant", "PT Serif", ...;
```

## Изображения

Все иллюстрации выгружены из Figma и **лежат в репозитории**, а не подтягиваются по ссылке: ссылки на ассеты Figma живут около семи дней.

Исходники весили 26 МБ. После пересчёта под реальные размеры отображения (с двойным запасом под retina) и перевода в WebP осталось 1.32 МБ — это 95% экономии.

Маски `*-m.png` — служебные, обрезают съедобные значки в блоке про забавы.

## Анимации

Появление блоков по мере прокрутки: `IntersectionObserver` вешает класс `.is-in`, дальше работает CSS-переход. Порядок внутри группы задаётся атрибутом `data-delay` в миллисекундах.

Связка баранок покачивается на верёвке после появления.

Всё это выключается при `prefers-reduced-motion: reduce` — у кого в системе стоит запрет на анимацию, увидит статичную страницу без рывков.

## Разработка

Отдельной сборки нет, это статические файлы. Достаточно поднять любой локальный сервер:

```powershell
python -m http.server 8000
```

и открыть `http://localhost:8000`. Открывать `index.html` двойным щелчком не стоит — из-за `file://` не отработают маски у значков.

## Публикация

Хостится на GitHub Pages, ветка `main`, корень репозитория.

---

by MRWLTR
